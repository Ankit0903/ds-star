# config.yaml
#
# API keys should be provided via environment variables (recommended):
# - OpenAI:   OPENAI_API_KEY
# - Gemini:  GEMINI_API_KEY
# - Ollama:  OLLAMA_API_KEY (optional)
#
# If you really want to provide an API key via this file, use:
# api_key: "..."
# run_id: "experiment_name"  # Leave commented to auto-generate unique run IDs
model_name: "gpt-4-turbo-preview"  # Valid model for Assistants API
interactive: false
max_refinement_rounds: 3
preserve_artifacts: true